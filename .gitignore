# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Jupyter
.ipynb_checkpoints/

# Data outputs (generated; not tracked)
data/outputs/
# Normalized metric panel: large (~56MB) regenerable build artifact
# (deterministic output of src/build_normalized_panel.py from committed inputs)
data/processed/normalized_panel.csv

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets
.env
*.key

# Large raw data files
data/raw/*.csv
data/raw/*.zip
data/raw/*.xls
data/raw/*.xlsx
!data/raw/download_log.csv

# Machine-specific environment variables
.env

.virtual_documents/
notebooks/**/Untitled*.ipynb

data/processed/_*.txt

# Step-0 metric inventory: early profiling artifact, read by no pipeline code
# (superseded by metric_distribution_profile.csv + metric_selection.csv)
data/processed/step0_metric_inventory.csv
